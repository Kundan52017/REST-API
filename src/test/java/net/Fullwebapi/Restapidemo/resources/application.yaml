spring:
 datasource:
   url: jdbc:h2://men:db;DB_CLOSE_DELAY=-1
   username: root
   password: Kundan74@
   driver-class-name: org.h2.Driver

   # the sql dialect mokes hibernate generate better sql for the chosen database
   
   jpa:
     properties:
      hibernate:
       dialect: org.hibernate.dialect.H2Dialect
       show-sql: true
       
       #Jpa setting
       
 jpa:
  hibernate:
    ddl-auto: create-drop
